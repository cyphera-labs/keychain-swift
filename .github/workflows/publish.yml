name: Publish

on:
  push:
    tags: ['v*']

jobs:
  publish:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build
        run: swift build -c release

      - name: Test
        run: swift test
